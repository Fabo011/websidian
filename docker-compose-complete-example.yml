# Complete docker-compose for self-hoster (PostgreSQL + tunnel).
#
# ⚠️ HTTPS IS REQUIRED. Serve the app over https:// (the tunnel / reverse proxy
# below terminates TLS). This is not only for security: end-to-end encryption
# uses the browser WebCrypto API (crypto.subtle), which browsers ONLY expose in
# a "secure context" — HTTPS (or localhost). Over plain HTTP on a real domain/IP
# the crypto API is undefined and the app breaks, notably on smartphones. Make
# sure APP_URL / CORS_ORIGINS use https:// and the tunnel serves a valid cert.
services:

  # Cloudflare is only an example, you can use what ever you want, nginx config or other zero trust tunnel provider.
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TOKEN}
    
  websidian:
    image: ghcr.io/fabo011/websidian:latest
    container_name: websidian
    restart: unless-stopped
    ports:
      - "3065:3065"
    environment:
      USER_STORAGE_ENABLED: "true"
      ALLOW_REGISTRATION: "${ALLOW_REGISTRATION:-true}" # Change to false after you and your friends have registered to prevent random people from registering
      PORT: 3065
      ENCRYPTION_ENABLED: "${ENCRYPTION_ENABLED:-true}"
      ENCRYPTION_KEY: "${ENCRYPTION_KEY:-unsecureDefaultPleaseChangeMe}"
      JWT_EXPIRES_IN: "${JWT_EXPIRES_IN:-7d}"
      JWT_SECRET: "${JWT_SECRET:-unsecureDefaultPleaseChangeMe}"
      APP_URL: "${APP_URL}" # Frontend url, your domain
      CORS_ORIGINS: "${CORS_ORIGINS}" # Frontend url, your domain
      DB_TYPE: "${DB_TYPE:-postgres}"
      DB_HOST: "${DB_HOST:-localhost}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_USERNAME: "${DB_USERNAME:-postgres}"
      DB_PASSWORD: "${DB_PASSWORD:-postgres}" # Change
      DB_DATABASE: "${DB_DATABASE:-web_obsidian}"
      DB_SSL: "${DB_SSL:-false}"
      TRASH_RETENTION_DAYS: "${TRASH_RETENTION_DAYS:-7}"
      RATE_LIMIT_ENABLED: "${RATE_LIMIT_ENABLED:-true}"
      RATE_LIMIT_WINDOW_SECONDS: "${RATE_LIMIT_WINDOW_SECONDS:-60}"
      RATE_LIMIT_MAX: "${RATE_LIMIT_MAX:-500}"
      VAULT_TREE_CACHE_SECONDS: "${VAULT_TREE_CACHE_SECONDS:-30}"
      MAX_UPLOAD_SIZE_MB: "${MAX_UPLOAD_SIZE_MB:-100}"
      IMPRINT: "${IMPRINT:-false}"
      AGB: "${AGB:-false}"
      LEGAL_NOTICE: "${LEGAL_NOTICE:-false}"
      MAX_UPLOAD_FILE_MB: "${MAX_UPLOAD_FILE_MB:-200}"
      MAX_IMPORT_FILES: "${MAX_IMPORT_FILES:-20000}"
      MAX_IMPORT_TOTAL_MB: "${MAX_IMPORT_TOTAL_MB:-200}"
      UPLOAD_REQUEST_TIMEOUT_MIN: "${UPLOAD_REQUEST_TIMEOUT_MIN:-15}"
      SEARCH_CACHE_TTL_MS: "${SEARCH_CACHE_TTL_MS:-15000}"
      RATE_LIMIT_DASH_ENABLED: "${RATE_LIMIT_DASH_ENABLED:-true}"
      RATE_LIMIT_DASH_WINDOW_SECONDS: "${RATE_LIMIT_DASH_WINDOW_SECONDS:-60}"
      RATE_LIMIT_DASH_MAX: "${RATE_LIMIT_DASH_MAX:-50000000}"
      UPLOAD_EXCLUDE_PATTERNS: "${UPLOAD_EXCLUDE_PATTERNS:-.DS_Store,Thumbs.db,*.tmp}" # e.g. .DS_Store,Thumbs.db,*.tmp
      MAX_OPEN_TABS: "${MAX_OPEN_TABS:-8}"
      CHAT_ENABLED: "${CHAT_ENABLED:-true}"
      CHAT_PENDING_TTL_DAYS: "${CHAT_PENDING_TTL_DAYS:-30}"
    depends_on:
      - cloudflared
      - postgres

  postgres:
    image: postgres:18-alpine
    container_name: websidian-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: "${DB_USERNAME}"
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
      POSTGRES_DB: "${DB_DATABASE}"
    volumes:
      - postgres-data:/var/lib/postgresql
    depends_on:
      - cloudflared

  nextcloud:
      image: nextcloud:latest
      container_name: nextcloud
      restart: always
      ports:
        - "8084:80"
      volumes:
        - /mnt/ssd/nextcloud/html:/var/www/html # Change to your mount
        - /mnt/ssd/nextcloud/data:/var/www/html/data # Change to your mount
      environment:
        TZ: Europe/Berlin
        PHP_MEMORY_LIMIT: 1G
        PHP_UPLOAD_LIMIT: 1G
        OVERWRITEPROTOCOL: https
        TRUSTED_PROXIES: 172.16.0.0/12 10.0.0.0/8 192.168.0.0/16
        NEXTCLOUD_TRUSTED_DOMAINS: websidian.fabo011-cloud.de # Change to your domain
      mem_limit: 1536m
      memswap_limit: 2g  

volumes:
  postgres-data:
